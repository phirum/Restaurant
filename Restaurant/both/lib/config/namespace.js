/**
 * Namespace
 */
Restaurant = {};

Meteor.isClient && Template.registerHelper('Restaurant', Restaurant);

/* Collection */
Restaurant.Collection = {};

/* Schema */
Restaurant.Schema = {};

/* Tabular */
Restaurant.TabularTable = {};
